#pragma once

#include <cstdint>

namespace propnet
{
    // TODO
    template<typename T>
    class PersistentArray
    {
        public:
            T at(std::size_t idx) const
            {
                return T {};
            }
        private:
    };
};
